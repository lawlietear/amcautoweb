# 山东金融资产智能文书系统 - 开发文档

> 本文档面向开发者，记录系统架构、数据结构、实现细节等设计信息。
>
> 更新时间：2026-03-29

---

## 1. 项目概况

| 项目 | 内容 |
|------|------|
| **项目名称** | 山东金融资产智能文书系统（原AMC文书生成器） |
| **技术栈** | Flask + Handsontable 14.0.0 + python-docx/docxtpl + pandas |
| **运行方式** | `python app.py` → http://localhost:5000 |
| **当前版本** | v2.0.0 |

---

## 2. 系统架构

### 2.1 功能模块

```
┌─────────────────────────────────────────┐
│           首页 (index.html)              │
│  ├─ 部门选择：风险合规部 / 业务部         │
│  ├─ 审计日志入口（右下角浮动按钮）        │
│  └─ Logo + 系统标题                      │
└─────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        ▼                       ▼
┌───────────────┐      ┌───────────────┐
│  风险合规部    │      │    业务部      │
│ (批复文档)    │      │  (合同文档)    │
│               │      │               │
│ 单表格模式    │      │ 双表格模式    │
│ 24个变量      │      │ 30个变量      │
└───────────────┘      └───────────────┘
```

### 2.2 双部门对比

| 部门 | 功能 | 变量数 | 表格模式 | 模板文件 |
|------|------|--------|---------|---------|
| **风险合规部** | 批复文档生成 | 24个 | 单表格 | 一次性业务批复、分期业务批复、收购业务批复 |
| **业务部** | 合同文档生成 | 30个 | 双表格 | 债权转让协议（分期/非分期） |

---

## 3. 数据结构设计

### 3.1 业务部变量（30个）

**批复信息表（9列）**：
- 基准日、债权本金金额、债权利息金额、其他费用金额
- 债权总额、保证金金额、转让价格、剩余交易价款、资金占用费率

**合同信息表（21列）**：
- 乙方名称、法定代表人、注册地
- 合同编号、收款银行、银行账户名称、银行账号
- 甲方联系人/电话/传真/地址/邮编/邮箱
- 乙方联系人/电话/传真/地址/邮编/邮箱
- 资金占用起算日、违约金比例（10.3.2条款）、违约金比例（10.3.3条款）

### 3.2 风险合规部变量（24个）

- 批复编号、会议日期、会次、业务分类、项目名称
- 审核人员、尽调报告名称、部门、项目负责人
- 债务人名称、户数、基准日
- 债权本金金额、债权利息金额、其他费用金额、债权总额
- 拟转让价格、交易费用承担主体、保证金金额、剩余交易价款
- 尾款支付期限、资金占用费率

### 3.3 金额字段处理（自动格式化）

```python
# 自动格式化千分位 + 生成中文大写
amount_fields = [
    'principal_amount',      # → principal_cn
    'interest_amount',       # → interest_cn
    'other_fees_amount',     # → other_fees_cn
    'total_claim_amount',    # → total_claim_cn
    'deposit_amount',        # → deposit_cn
    'transfer_price',        # → transfer_price_cn
    'remaining_price'        # → remaining_cn
]
```

### 3.4 百分比字段清理

```python
percent_fields = [
    'interest_rate',
    'penalty_percent_10_3_2',
    'penalty_percent_10_3_3'
]
# 用户填写 "8%/年" → 自动清理为 "8"
```

---

## 4. 合同变量映射分析

### 4.1 模板变量统计

| 模板类型 | 变量数量 |
|---------|---------|
| 分期合同 | 21个 |
| 非分期合同 | 30个 |
| 两模板共有 | 19个 |
| 仅分期特有 | 2个 (interest_rate, funding_start_date) |
| 仅非分期特有 | 11个 (party_a_*, party_b_*, bank_name等) |

### 4.2 重合变量（批复↔合同）

这些变量在批复和合同中都存在，可以直接从批复页面复制粘贴：

| 变量名 | 含义 | 批复中有 | 合同中有 |
|-------|------|---------|---------|
| base_date | 基准日 | ✅ | ✅ |
| principal_amount | 债权本金 | ✅ | ✅ |
| interest_amount | 利息金额 | ✅ | ✅ |
| other_fees_amount | 其他费用 | ✅ | ✅ |
| total_claim_amount | 债权总额 | ✅ | ✅ |
| deposit_amount | 保证金 | ✅ | ✅ |
| party_b_name | 乙方名称（债务人） | ✅(debtor_name) | ✅ |

---

## 5. 关键实现细节

### 5.1 智能模板选择逻辑

```python
if 'occupancy_rate' in form_data and form_data['occupancy_rate'].strip():
    auto_detected_template = 'fenqi'  # 分期
else:
    auto_detected_template = 'yicixing'  # 一次性
```

### 5.2 双表格行数同步（JavaScript）

```javascript
function addRowDual() {
    const pifuData = hotPifu.getData();
    const hetongData = hotHetong.getData();
    pifuData.push(new Array(pifuData[0].length).fill(''));
    hetongData.push(new Array(hetongData[0].length).fill(''));
    hotPifu.loadData(pifuData);
    hotHetong.loadData(hetongData);
}
```

### 5.3 金额大写转换修复记录

**问题**：`amount_to_chinese` 函数对"万"位处理有bug，10000000输出"壹仟元"而非"壹仟万元"

**解决**：重写 `_int_to_chinese` 和 `_four_digit_to_chinese` 方法，正确分组处理万、亿级数字

---

## 6. 文件结构

```
autodocweb_v2/
├── app.py                      # Flask主程序
├── core/                       # 核心模块
│   ├── __init__.py
│   ├── document_generator.py   # Word文档生成
│   ├── format_utils.py         # 金额格式化、日期转换
│   ├── excel_handler.py        # Excel导入处理
│   └── notification.py         # 审计日志记录
├── config/                     # 配置文件
│   ├── config_risk_compliance.json  # 风险合规部配置（24变量）
│   └── config_business.json         # 业务部配置（30变量）
├── templates/                  # HTML模板
│   ├── index.html              # 首页
│   ├── dept_form.html          # 数据录入页（单表格/双表格）
│   ├── audit_log.html          # 审计日志页
│   └── audit_login.html        # 登录页
├── static/                     # 静态资源
│   ├── css/style.css           # 样式文件
│   └── images/logo.svg         # Logo
├── word_templates/             # Word模板
│   ├── template_一次性业务批复.docx
│   ├── template_分期业务批复.docx
│   ├── template_收购业务批复.docx
│   ├── template_收购拟处置业务批复.docx
│   ├── template_债权转让协议_非分期.docx
│   └── template_债权转让协议_分期处置.docx
├── generated/                  # 生成文件输出
│   ├── 风险合规部/
│   └── 业务部/
└── docs/                       # 项目文档
    ├── 用户手册.md
    ├── 部署指南.md
    └── 开发文档.md             # 本文件
```

---

## 7. 关键配置

### 7.1 后端配置（app.py）

```python
# 审计日志密码
AUDIT_PASSWORD = 'amc2026'

# 部门配置
DEPT_CONFIG = {
    'risk_compliance': {
        'config_file': 'config_risk_compliance.json',
        ...
    },
    'business': {
        'config_file': 'config_business.json',
        ...
    }
}
```

### 7.2 业务部默认值保存（LocalStorage）

**固定变量清单**（8个）：
```
party_a_contact, party_a_phone, party_a_fax,
party_a_address, party_a_zip, party_a_email,
penalty_percent_10, penalty_percent_20
```

---

## 8. 访问地址

| 页面 | 地址 |
|------|------|
| 首页 | http://localhost:5000 |
| 风险合规部 | http://localhost:5000/risk_compliance |
| 业务部 | http://localhost:5000/business |
| 审计日志 | http://localhost:5000/audit_log（密码：amc2026） |

---

## 9. 开发历史

- **2026-03-12** (v2.0.0)：多用户并发支持、Waitress生产服务器、收购业务模板
- **2026-03-09**：业务部界面重构（双表格设计）、默认值保存功能
- **2026-03-01** (v1.0.0)：初始版本发布

---

*本文档合并自：PROJECT_SUMMARY.md, PROJECT_MEMORY.md, SESSION_HANDOVER.md, 合同变量分析报告.md*
